import { heroui } from "@heroui/react";
export default heroui({
    themes: {
        light: {
            colors: {
                // primary: "#008000",
            }
        },
        dark: {
            colors: {
                // primary: "#FF0000",
            }
        },
    },
});
